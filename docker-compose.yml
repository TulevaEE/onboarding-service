version: '2'
services:
  database:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=tuleva-onboarding
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_DB=tuleva-onboarding
